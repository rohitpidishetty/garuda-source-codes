# Fetching even numbers ranging from 1 to 100, and storing it in a list.

main() {
  list<int> evenNumbers = new list<>()
  for number in (1, 101, 1) {
    result = number % 2
    isEven = result == 0
    if(isEven) {
      evenNumbers.push(number)
    }
  }
  evenNumbers.print()
}